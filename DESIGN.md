# DESIGN.md

## 1. 원칙

- 고급스럽고 세련되되 복잡함은 줄인다. 가독성이 항상 최우선.
- AI 특유의 디자인 배제: 과한 그라데이션, 보라·네온 계열, 유리morphism, 이모지 남발 금지.
- 화면의 주인공은 UI가 아니라 숫자와 문장이다.
- **세련됨은 장식이 아니라 위계와 리듬에서 나온다.** 요소를 더하기 전에 크기·여백·정렬을 먼저 정리한다.

## 2. 컬러

| 토큰 | 값 | 용도 |
|---|---|---|
| `bg` | `#FCFCFA` | 바탕. 순백을 쓰지 않는다 |
| `surface` | `#F5F4F1` | 면 분리. 시장 상황, 자산군 비중처럼 묶어야 할 블록 |
| `ink` | `#0A0A0A` | 제목, 강조 숫자 |
| `fg` | `#1F2023` | 본문 |
| `muted` | `#55585E` | 보조 문장 |
| `faint` | `#8A8F98` | 라벨, 캡션 |
| `line` | `#E4E3DE` | 항목 구분선 |
| `line-strong` | `#CFCEC8` | 구조 구분선 |
| `gain` | `#DC2626` | 상방, 수익 |
| `loss` | `#2563EB` | 하방, 손실 |

- **유채색은 적/청 둘뿐.** 그 외 유채색과 그라데이션은 금지.
- **적/청은 "손익"이 아니라 "방향"을 뜻한다.** 지표 변화값, 자산군 상방·하방에도 쓴다.
- **형광펜(`mark`, 2026-08-26)은 방향색의 예외가 아니라 다른 채널이다.** 글자색이 아니라
  배경이고, 노랑이라 적/청과 헷갈리지 않는다. 용법은 단 하나: **화면에 한 곳,
  오늘의 톱의 핵심 한 줄에만.** 두 곳부터는 아무것도 강조가 아니다.
  색이 두 가지를 뜻하는 순간 적과 청이 방향으로 안 읽힌다. **자산군마다 색을 주는 짓은 하지 않는다.**
- **순백(`#FFFFFF`)을 바탕으로 쓰지 않는다.** "아직 디자인하지 않음"으로 읽힌다. 따뜻한 오프화이트로 종이 느낌을 만든다.
- **무채색은 4단계로 쓴다.** 단계가 둘뿐이면 위계를 크기로만 만들어야 해서 화면이 평평해진다.
- **선 색을 반드시 명시한다.** Tailwind v4의 기본 border 색은 `currentColor`라 `border-line`을 빼면 검은 선이 그려진다.
- **다크모드는 만들지 않는다** (2026-08-26 결정). 색이 전부 토큰이라 마음이 바뀌면 값만 갈아끼우면 된다.

## 3. 타이포그래피

**서체 두 벌.**

- **Pretendard**: 본문, 숫자, UI 전부. 자릿수 정렬이 필요한 곳은 무조건 이쪽.
- **Noto Serif KR** (`font-serif`): 브리핑 제목, 마스트헤드, 섹션 제목. **본문에는 쓰지 않는다.**
  매일 10분 읽을 글이라 화면 가독성이 우선이다. 명조는 이 화면이 관리자 도구가 아니라
  출판물이라는 신호를 주는 장치다.

- 숫자에는 **`tabular` 유틸 필수** (`font-variant-numeric: tabular-nums`).
  같은 표가 반복되면 `table-fixed`로 열 폭을 고정한다. 표마다 열 위치가 어긋나면 자릿수 비교가 무의미해진다.
- 위계는 크기와 굵기로만 만든다. 색으로 위계를 만들지 않는다.

**타이포 스케일.** `globals.css`의 `@theme`에 정의돼 있다.

| 토큰 | 크기 | 행간 | 굵기 | 용도 |
|---|---|---|---|---|
| `text-display` | 44px | 1.1 | 700 | 총자산, 브리핑 날짜 |
| `text-title` | 28px | 1.25 | 700 | 페이지·섹션 제목 |
| `text-lead` | 23px | 1.4 | 600 | **각 섹션의 첫 기사 전용** |
| `text-heading` | 21px | 1.4 | 600 | 기사 제목, 지표 값 |
| `text-subhead` | 15px | 1.45 | 600 | 자산군명, 항목명 |
| `text-body` | 17px | 1.8 | 400 | 브리핑 본문 |
| `text-small` | 15px | 1.7 | 400 | 보조 문장 |
| `label` | 13px | 1.5 | 600, 자간 0.04em, faint | 라벨·캡션 (유틸) |

- **임의값 금지.** `text-[17px]` 같은 값을 쓰지 않는다. 스케일에 없는 크기가 필요하면 스케일을 고친다.
- 큰 글자는 자간을 좁힌다. 한글은 크게 키우면 자간이 벌어져 보인다.
- **위아래 단계 차이를 충분히 벌린다.** 섹션 제목과 본문이 3px 차이면 그건 위계가 아니다.
- **작은 쪽을 무한정 줄여 대비를 만들지 않는다.** 한글은 같은 px에서 라틴보다 작아 보인다.
  라벨 11px은 읽히지 않는다. 13px이 하한이고, 자간도 0.04em을 넘기지 않는다.

## 4. 레이아웃

- 카드 남발 금지. 구분은 여백과 얇은 선(`line` 1px)으로. 묶어야 할 때는 `surface` 면을 쓴다.
- 그림자 최소화, 라운드 4~8px 이하.
- 브리핑 화면은 신문처럼: 마스트헤드로 시작을 알리고, 명확한 타이포 위계, 넓은 여백.
- **브리핑은 1면과 리더의 이원 체제다** (2026-08-26 확정. 아코디언 등 중간 형태를 정리했다).
  - **1면은 훑는 화면이다.** 상세를 싣지 않는 대신 모든 헤드라인이 한 화면에 들어온다.
    마스트헤드(날짜 + 읽음 n/N + "오늘 브리핑 읽기" CTA) → 오늘의 톱 → **2×2 분면 그리드**
    → 자산군 종합. 분면은 `surface` 면 위 헤드라인 목록이다(첫 기사만 크게).
  - **두 축이 화면 기하다.** 왼쪽 열 국내, 오른쪽 열 국제, 윗줄 경제, 아랫줄 정치·사회.
  - **오늘의 톱 1건**만 1면에서 전문이 실린다. `display` 헤드라인, 대표 숫자 크게(방향색),
    핵심 한 줄 형광펜. 모델이 고르고 코드가 검증한다. 중요한 것은 클릭 없이 읽혀야 한다.
  - **리더는 정독하는 화면이다.** 불렛·맥락·용어는 전부 여기서만. 카드 한 장에 한 건,
    옆으로 넘긴다(스와이프·방향키·버튼). 넘김이 곧 읽음이고 진행 바가 쌓인다.
    마지막에는 **완료 카드**("N건 다 읽었다" + 자산군 방향 요약)로 마침표를 찍는다.
  - **카드의 골격은 셋으로 고정이다** (2026-08-26 확정): **내용 → 인사이트 → 출처.**
    내용은 문장체 문단 3~5개(스트레이트 기사체), **인사이트는 `surface` 면 박스**(라벨 +
    개조식 불렛 1~4개. 신문의 '해설 박스' 은유 — 서사와 해석은 면으로 갈라져야 경계가 보인다),
    출처·이슈는 각자 한 줄. 경제·정치 구분 없이 같은 골격.
    구조가 항목마다 다르면 사용자는 화면을 불신한다. 원문 조각을 직접 싣지 않는다.
    재료(기사 본문)는 모델에 전부 먹여 우리 문체로 소화한다.
  - **1면 톱은 2단이다** (2026-08-26): 왼쪽 = 내용 문단(프로즈 폭), 오른쪽 레일(border-l) =
    대표 숫자(크게, 방향색) + 인사이트 박스. stat이 없는 날도 레일이 비지 않는다.
    모바일은 숫자 → 내용 → 인사이트 세로 스택.
  - **가이드 줄글 금지.** "~하면 ~된다" 식 안내 문장을 화면에 쓰지 않는다.
    빈 상태·부가 설명은 단어형 라벨로만 ("쌓인 이슈 없음", "전개 1회").
    조작법은 화면이 아니라 affordance가 가르친다.
  - **affordance는 하나다. 헤드라인은 어디서든 누르면 그 항목의 카드가 열린다.**
    1면 목록, 톱, 자산군 근거까지 같은 동작. 어떤 헤드라인은 눌리고 어떤 것은 죽어 있으면
    사용자는 화면 전체를 불신한다.
  - **읽음 체크**: 항목마다 작은 원. 카드로 보면 자동으로 채워지고 읽은 헤드라인은 `muted`로
    가라앉는다. 저장은 localStorage, 날짜가 바뀌면 리셋.
  - 출처와 이슈는 항목 하단에 **각자 한 줄씩** 둔다 (2026-08-26). 가운뎃점으로 이질적
    정보를 한 줄에 잇는 것은 AI 티가 나는 지점이라 금지. 종류가 다르면 줄을 바꾼다.
  - "시장 예상치 확인 불가"는 정보가 아니므로 저장만 하고 싣지 않는다.
  - 한국어 헤드라인에는 `break-keep` 필수. 단어 중간 줄바꿈은 조판 사고다.
- **라벨을 남발하지 않는다.** 문단마다 이름표가 붙으면 기사가 아니라 서류로 읽힌다.
- **브리핑과 포트폴리오는 다른 옷을 입는다.** 브리핑은 출판물(명조 제목, 넓은 행간),
  포트폴리오는 계측기(큰 숫자, 표, 방향색). 둘이 같아 보이면 둘 다 어중간해진다.
- **축이 있으면 축이 보여야 한다.** 브리핑은 국내와 국제를 두 열로 나란히 세운다.
  4분면으로 나눠놓고 세로로 일렬로 늘어놓으면 축이 화면에서 사라진다.
  - 각 열은 `container-prose`(42rem)를 넘지 않게 유지한다. 열 폭이 프로즈 한계를 넘으면 읽기가 나빠진다.
  - 좁은 화면(`lg` 미만)에서는 한 열로 이어 붙인다.
- **1차 축을 2차 축보다 크게 둔다.** 국내/국제가 `title`, 경제/정치·사회가 `subhead`다.
  이 순서가 뒤집히면 무엇이 상위 구분인지 화면이 거짓말을 한다.
- **좌우 여백이 화면의 3분의 1을 넘지 않게 한다.** 읽는 화면이라도 1920px에서
  콘텐츠가 절반만 차지하면 미완성으로 보인다. 폭이 남으면 열을 늘려 쓴다.
- **컨테이너 폭 3종만 쓴다.**

  | 토큰 | 폭 | 용도 |
  |---|---|---|
  | `max-w-prose` | 42rem | 읽는 본문 |
  | `max-w-page` | 88rem | 페이지 전체, 2열 레이아웃 |
  | `max-w-form` | 26rem | 로그인, 입력 폼 |

- **모바일 우선** (PWA 주 사용 환경).
  - 표를 가로 스크롤하게 두지 않는다. 모바일에서는 행 단위 스택으로 바꾼다.
  - 고정 폭은 `lg:` 이상에만 건다.
  - 라벨 위치는 내용 길이가 정한다. 내용이 문장이면 라벨을 위에, **한 줄 명사구면 데스크탑에서 좌측 열**에 둔다.
    개조식은 내용이 짧아 좌측 라벨이 스캔에 유리하다. 모바일은 어느 쪽이든 라벨을 위로 올린다.
- 전역 마스트헤드는 모든 화면에서 같은 자리에 같은 모양으로 있어야 한다. 페이지마다 헤더를 따로 그리지 않는다.

## 5. 문체 규칙 (AI 생성 텍스트 포함 전체 적용)

- 단문, 두괄식, 숫자 우선.
- 금지 표현: "~할 수 있습니다", "주목됩니다", "~로 보입니다", "귀추가 주목", "다양한", "혁신적인" 류의 물기 없는 상투어 전부.
- 뭉개는 표현 금지: "~될 수 있다", "~할 가능성이 있다", "~가능성이 높다", "~로 풀이된다". 근거가 약하면 약한 대로 무엇을 모르는지 쓴다.
- **대시(—, –)로 문장을 잇지 않는다.** AI가 쓴 티가 가장 크게 나는 지점이다. 부연이 필요하면 문장을 끊거나 괄호를 쓴다.
  - 나쁜 예: "코스피 3% 하락 — 반도체 약세 탓이다"
  - 좋은 예: "코스피가 3% 하락했다. 반도체 약세 탓이다"
  - 값이 없는 칸은 대시 대신 가운뎃점(`·`)으로 표시한다.
- **보조 항목은 개조식으로 쓴다.** 자산군 함의처럼 짧게 나열되는 것은 한 줄 40자 이내, "~함"이나 명사구로 끝낸다.
  - 사실·서프라이즈·맥락·전망은 문장체를 유지한다. 문장이라야 맥락이 전달된다.
- 단정할 근거가 없으면 단정하지 않되, 애매하게 뭉개지도 않는다. "예상 부합 / 상회 / 하회"처럼 기준 대비로 말한다.
- **온점을 최소화한다.** 개조식 항목은 온점 없이 끝낸다. 문장체로 쓰는 곳에서만 온점을 쓴다.
- 이모지 사용 금지 (알림 배지 등 기능적 표시는 아이콘으로).

## 6. 컴포넌트 톤 참고

- **시장 상황 지표**: 숫자는 `heading`(사이드) 또는 `title`(홈), 라벨과 해석은 `label`.
  **변화값에는 방향색을 쓴다.** 안 움직인 지표는 변화값이 없어 자연히 조용해진다.
- **자산군 함의**: 자산군명은 `ink`, 방향은 방향색, 설명은 `muted` 한 줄 개조식.
  브리핑에 보유 자산을 끌어오지 않는다. 브리핑은 읽는 화면이고 포트폴리오는 계기판이다.
- **본문 불렛**: 브리핑 본문은 개조식 불렛이다. 화면에서 가장 큰 본문 크기(`text-body`)를 준다.
  마커는 가운뎃점 하나에 `faint` 색. 대시를 마커로 쓰지 않는다.
- **용어 밑줄**: 점선 밑줄만. 색을 쓰지 않는다. 팝오버는 1px 테두리와 배경색만, 그림자 없음.
- **이슈 배지**: 다른 보조 항목과 같은 라벨 행에 둔다. 배지처럼 튀게 만들지 않는다.
- **비중 막대**: 가는 수평 막대. 길이로만 나타내고 자산군에 색을 주지 않는다.
- **링크**: 밑줄 + `underline-offset-4`, 기본은 `faint`, hover에서 `ink`. 버튼처럼 보이게 만들지 않는다.
- **현재 위치 표시**: 굵기와 밑줄로만. 색으로 상태를 만들지 않는다.
