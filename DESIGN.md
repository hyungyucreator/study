# DESIGN.md

## 1. 원칙

- 고급스럽고 세련되되 복잡함은 줄인다. 가독성이 항상 최우선.
- AI 특유의 디자인 배제: 과한 그라데이션, 보라·네온 계열, 유리morphism, 이모지 남발 금지.
- 화면의 주인공은 UI가 아니라 숫자와 문장이다.
- **세련됨은 장식이 아니라 위계와 리듬에서 나온다.** 요소를 더하기 전에 크기·여백·정렬을 먼저 정리한다.

## 2. 컬러

| 토큰 | 값 | 용도 |
|---|---|---|
| `bg` | `#FFFFFF` | 배경 |
| `surface` | `#FAFAFA` | 면 분리. 시장 상황, 자산군 비중처럼 묶어야 할 블록 |
| `fg` | `#111111` | 본문 텍스트 |
| `muted` | `#6B7280` | 보조 텍스트 |
| `line` | `#E5E7EB` | 구분선 1px |
| `gain` | `#DC2626` | 수익 (국내 관습: 적) |
| `loss` | `#2563EB` | 손실 (국내 관습: 청) |

- **이 일곱 색 외 사용 금지.** 그라데이션 일절 금지.
- `surface`는 카드와 그림자를 대신하는 수단이다. 면으로 묶되 띄우지 않는다.
- **선 색을 반드시 명시한다.** Tailwind v4의 기본 border 색은 `currentColor`라 `border-line`을 빼면 검은 선이 그려진다.
- 다크모드는 추후 과제 (초기 미구현). 색은 전부 토큰으로 빼두어 값만 갈아끼울 수 있게 한다.

## 3. 타이포그래피

- 폰트: **Pretendard 단일.** 웹폰트 로드, fallback은 시스템 산세리프.
- 숫자에는 **`tabular` 유틸 필수 적용** (`font-variant-numeric: tabular-nums`). 대시보드 가독성의 핵심이라 자릿수 정렬이 어긋나면 안 된다.
  - 같은 표가 여러 번 반복되면 `table-fixed`로 열 폭을 고정한다. 표마다 열 위치가 어긋나면 자릿수 비교가 무의미해진다.
- 위계는 크기와 굵기로만 만든다. 색으로 위계를 만들지 않는다.

**타이포 스케일 7단계.** `globals.css`의 `@theme`에 정의돼 있다.

| 토큰 | 크기 | 행간 | 굵기 | 용도 |
|---|---|---|---|---|
| `text-display` | 34px | 1.15 | 600 | 총자산, 브리핑 날짜 |
| `text-title` | 24px | 1.3 | 600 | 페이지 제목, 1부/2부 |
| `text-heading` | 19px | 1.4 | 600 | 기사 제목, 지표 값 |
| `text-subhead` | 15px | 1.45 | 600 | 자산군명, 항목명 |
| `text-body` | 16px | 1.75 | 400 | 브리핑 본문 |
| `text-small` | 14px | 1.65 | 400 | 보조 문장 |
| `label` | 12px | 1.5 | 500, 자간 0.06em, muted | 라벨·캡션 (유틸) |

- **임의값 금지.** `text-[17px]` 같은 값을 쓰지 않는다. 스케일에 없는 크기가 필요하면 스케일을 고친다.
- `display`와 `title`은 자간을 좁힌다. 한글은 크게 키우면 자간이 벌어져 보인다.

## 4. 레이아웃

- 카드 남발 금지. 구분은 여백과 얇은 선(`line` 1px)으로. 묶어야 할 때는 `surface` 면을 쓴다.
- 그림자 최소화, 라운드 4~8px 이하.
- 브리핑 화면은 신문처럼: 마스트헤드로 시작을 알리고, 명확한 타이포 위계, 넓은 여백.
- **본문은 한 컬럼.** 기사 본문은 `container-prose`(42rem)를 넘지 않는다.
  - 넓은 화면(`lg` 이상)에서 **보조 정보는 사이드로 뺄 수 있다.** 시장 상황 지표, 목차처럼 본문 흐름과 독립적인 것에 한한다. 본문 자체를 두 단으로 나누지 않는다.
- **컨테이너 폭 3종만 쓴다.**

  | 토큰 | 폭 | 용도 |
  |---|---|---|
  | `max-w-prose` | 42rem | 읽는 본문 |
  | `max-w-page` | 68rem | 페이지 전체, 2단 레이아웃 |
  | `max-w-form` | 26rem | 로그인, 입력 폼 |

- **모바일 우선** (PWA 주 사용 환경).
  - 표를 가로 스크롤하게 두지 않는다. 모바일에서는 행 단위 스택으로 바꾼다.
  - 고정 폭은 `lg:` 이상에만 건다.
  - 라벨은 좌측 열이 아니라 내용 위에 둔다. 좁은 화면에서 좌측 라벨 열은 폭만 잡아먹는다.
- 전역 마스트헤드는 모든 화면에서 같은 자리에 같은 모양으로 있어야 한다. 페이지마다 헤더를 따로 그리지 않는다.

## 5. 문체 규칙 (AI 생성 텍스트 포함 전체 적용)

- 단문, 두괄식, 숫자 우선.
- 금지 표현: "~할 수 있습니다", "주목됩니다", "~로 보입니다", "귀추가 주목", "다양한", "혁신적인" 류의 물기 없는 상투어 전부.
- 뭉개는 표현 금지: "~될 수 있다", "~할 가능성이 있다", "~가능성이 높다", "~로 풀이된다". 근거가 약하면 약한 대로 무엇을 모르는지 쓴다.
- **대시(—, –)로 문장을 잇지 않는다.** AI가 쓴 티가 가장 크게 나는 지점이다. 부연이 필요하면 문장을 끊거나 괄호를 쓴다.
  - 나쁜 예: "코스피 3% 하락 — 반도체 약세 탓이다"
  - 좋은 예: "코스피가 3% 하락했다. 반도체 약세 탓이다"
  - 값이 없는 칸은 대시 대신 가운뎃점(`·`)으로 표시한다.
- **보조 항목은 개조식으로 쓴다.** 자산군 함의처럼 짧게 나열되는 것은 한 줄 40자 이내, "~함"이나 명사구로 끝낸다.
  - 사실·서프라이즈·맥락·전망은 문장체를 유지한다. 문장이라야 맥락이 전달된다.
- 단정할 근거가 없으면 단정하지 않되, 애매하게 뭉개지도 않는다. "예상 부합 / 상회 / 하회"처럼 기준 대비로 말한다.
- 이모지 사용 금지 (알림 배지 등 기능적 표시는 아이콘으로).

## 6. 컴포넌트 톤 참고

- **시장 상황 지표**: `surface` 면 위에, 숫자는 `heading`, 라벨과 해석은 `label`.
- **자산군 함의**: 좌측 2px 세로선으로 분리. 자산군명은 `subhead`, 설명은 `small` + muted 한 줄.
- **비중 막대**: 가는 수평 막대. 길이로만 나타내고 자산군에 색을 주지 않는다.
- **링크**: 밑줄 + `underline-offset-4`, 기본은 muted, hover에서 `fg`. 버튼처럼 보이게 만들지 않는다.
- **현재 위치 표시**: 굵기와 밑줄로만. 색으로 상태를 만들지 않는다.
