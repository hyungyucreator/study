# CLAUDE.md — 투자 데스크

이 파일은 이 저장소에서 작업하는 Claude가 매 세션 지켜야 할 원칙이다.
기획 문서와 충돌하면 기획 문서가 우선한다. 문서를 고치고 싶으면 **먼저 사용자에게 이유와 함께 묻는다.**

## 0. 제품 한 줄 정의

매일 10분 브리핑으로 세상(정치·사회·국제·테크)과 시장 국면을 읽고, 내 자산 현황을 확인하는 개인 대시보드.

**제품 범위는 두 가지뿐이다 — 학습과 현황 트래킹.**
- 학습: 데일리 브리핑(국내 경제 / 국제 경제 / 국내 정치·사회 / 국제 정치·사회 4섹션, 개조식),
  **이슈 스레드**(브리프 + 진행·주시·종결 상태), **개념 카드·단어장**, 이벤트 트리거 브리핑, 위클리 딥다이브
- 현황 트래킹: 보유 자산 조회(KIS + 수동), 총자산 추이, 자산군 비중, 종목별 수익률, 급변 알림

핵심 철학: **판단은 사용자가, 재료는 제품이.** 국면 읽기 중심. 과잉거래를 유발하지 않는다.
**판단 도구보다 이해 도구를 먼저** — 사용자가 스스로 근거를 갖기 전에 의사결정 기능을 붙이지 않는다.

## 1. 문서 라우팅 (작업 전 해당 문서를 읽는다)

| 질문 | 문서 |
|---|---|
| 이 기능을 만드나? 어떤 모습인가? | `PRODUCT.md` |
| 스택·데이터 소스·파이프라인·DB 스키마·비용·시크릿 | `ARCHITECTURE.md` |
| 색·폰트·레이아웃·문체 | `DESIGN.md` |
| 지금 무엇을 만들 차례인가 | `ROADMAP.md` |
| 무엇이 언제 왜 바뀌었나 | `CHANGELOG.md` |

## 2. 절대 금지 (Hard rules — 예외 없음)

1. **매매 실행 기능 금지.** 앱 내에서 주문을 내는 코드·API 호출·버튼을 만들지 않는다. 리밸런싱은 "주문서 생성 + 복사"까지만이고, 실제 주문은 사용자가 증권사 앱에서 직접 한다. (투자일임·자문업 라이선스 영역, 그리고 의도된 마찰)
2. **종목·상품 추천 문장 생성 금지.** 자산군 방향은 **브리핑 끝에서 한 번만 종합**하고 근거 기사를 함께 싣는다.
   항목마다 붙이면 모순이 흩어져 안 보인다. 단, **사후 검증은 허용한다** (브리핑이 말한 방향과 실제 움직임을 대조해 보여주는 것). 이건 조언이 아니라 검증이고, 도구를 덜 권위적으로 만든다. "이걸 사라", "지금 국면엔 이게 낫다", "A보다 B가 유리하다" 류의 문장을 코드로도 프롬프트로도 만들지 않는다. 숫자와 태그가 말하게 하고 판단은 사용자가 한다. 자산군 단위의 방향 함의까지만 허용하며, **개별 종목 함의는 금지.**
3. **기사 전문 수집·저장·표시 금지.** 저장 허용 범위는 제목 + 리드문 + 출처 링크 + 메타데이터뿐이다. 본문 전문을 긁거나 DB에 넣거나 화면에 렌더링하지 않는다. 세부 확인은 **링크아웃**으로만. 리드문만으로 해석이 어려운 기사는 제외하거나 짧게만 다룬다.
4. **HTML 크롤링 금지. 공식 API·RSS만.** 네이버 뉴스 페이지, Investing.com 등 크롤링 의존 기능은 만들지 않는다. 필요하면 공식 오픈 API(네이버 검색 API 등)를 쓰고, 없으면 그 기능을 포기하거나 수동 보완 테이블로 대체한다.
5. **시크릿의 클라이언트 노출 금지.** KIS 앱키/시크릿, Anthropic API 키, Supabase service_role 키, 텔레그램 봇 토큰은 **서버 환경변수 전용**이다. 이 값들에 `NEXT_PUBLIC_` 접두사를 붙이지 않는다. 클라이언트 번들·리포지토리·로그에 절대 넣지 않는다. 공개 가능한 값은 `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` 둘뿐이다.
6. **RLS 없는 유저 테이블 금지.** 유저별 테이블은 생성과 동시에 RLS 활성화 + `auth.uid() = user_id` 정책을 붙인다.

## 3. 만들지 않는 것 (PRODUCT.md §5 전문)

- 종목/상품 추천 문장 생성
- 앱 내 매매 주문 실행
- 퀴즈, 복습 시스템, 커리큘럼 트랙
- thesis 강제 입력 양식 (매매 메모는 선택적 한 줄만)
- 마이데이터/타 증권사 API 통합
- 국면 지표 전용 대시보드 화면 (브리핑 내 "시장 상황"으로 대체, 추후 사용자가 익숙해지면 재검토)
- 네이티브 앱 (웹 + PWA로 시작)

**범위 밖 — 보류 (2026-08-25 스코프 축소, PRODUCT.md §6):** 금지가 아니라 "지금 만들 때가 아닌 것"이다. 사용자가 스스로 목표를 정할 근거를 갖추기 전에 붙이면 제품이 준 숫자를 규칙처럼 따르게 된다.
- 목표 비중 설정 / 목표 vs 현재 비중 막대 / 리밸런싱 주문서 / 매매 시점 국면 스냅샷
- ETF 카탈로그 (코어·틸트 2층, 국면 태그)
- 버킷(core/tilt/experiment) UX — `holdings.bucket` 컬럼은 기본값 `core`로 남아 있으나 **화면에 노출하지 않는다**
- 이 기능들을 요청받으면 먼저 PRODUCT.md §6의 재논의 조건을 확인할 것.

추가로 지금 하지 않는 것: 전체 ETF DB, 웹소켓 초단위 시세, ETF 데이터 하드코딩.
**다크모드는 아예 만들지 않는다** (2026-08-26 결정).

## 4. 디자인 규칙 (DESIGN.md 요약. 어길 거면 먼저 묻는다)

- **유채색은 적/청 둘뿐.** 나머지는 무채색 토큰(`bg` `surface` `ink` `fg` `muted` `faint` `line` `line-strong`).
  **적/청은 "손익"이 아니라 "방향"을 뜻한다.** 지표 변화값, 자산군 상방·하방에도 쓴다.
  **자산군마다 색을 주지 않는다.** 색이 두 가지를 뜻하면 방향으로 안 읽힌다.
- **순백 바탕 금지.** `bg`는 `#FCFCFA`. 순백은 "아직 디자인 안 함"으로 읽힌다.
- **그라데이션 일절 금지.** 보라·네온 계열, 글래스모피즘 금지.
- **서체 두 벌.** Pretendard가 본문·숫자·UI 전부, Noto Serif KR(`font-serif`)은
  브리핑 제목·마스트헤드·섹션 제목 전용. **명조를 본문에 쓰지 않는다.**
- **숫자에는 `tabular` 유틸 필수.** 자릿수 정렬이 어긋나면 안 된다. 반복되는 표는 `table-fixed`.
- **작은 쪽을 무한정 줄여 대비를 만들지 않는다.** 라벨 하한 13px, 자간 0.04em.
- **축이 있으면 축이 보여야 한다.** 브리핑은 국내/국제 두 열. 1차 축을 2차 축보다 크게.
- **좌우 여백이 화면의 3분의 1을 넘지 않게 한다.** 폭이 남으면 열을 늘려 쓴다.
- **타이포는 8단계 스케일만 쓴다** (`display` / `title` / `lead` / `heading` / `subhead` / `body` / `small` / `label`).
  `text-[17px]` 같은 임의값 금지. 스케일에 없으면 스케일을 고친다.
- **각 부의 첫 기사는 `text-lead`로 크게 두고 라벨을 걷어낸다.** 다 똑같은 크기로 늘어놓으면
  무엇이 중요한지 화면이 말해주지 않는다.
- **컨테이너 폭은 3종만** (`max-w-prose` 42rem / `max-w-page` 68rem / `max-w-form` 26rem).
- 카드 남발 금지. 구분은 여백과 1px 선(`border-line`), 묶을 때는 `bg-surface` 면.
  **선 색을 반드시 명시한다** (Tailwind v4 기본 border 색은 currentColor라 검게 나온다).
- 본문은 한 컬럼. 넓은 화면에서 보조 정보(지표·목차)만 사이드로 뺀다.
- **모바일 우선.** 표를 가로 스크롤시키지 않고 행 단위로 스택한다. 고정 폭은 `lg:` 이상에만.
- **이모지 사용 금지** (기능적 표시는 아이콘으로).

## 5. 문체 규칙 (UI 문구·AI 생성 텍스트 전체 적용)

- 단문, 두괄식, 숫자 우선.
- 금지 표현: "~할 수 있습니다", "주목됩니다", "~로 보입니다", "귀추가 주목", "다양한", "혁신적인" 류의 상투어 전부.
- 뭉개는 표현 금지: "~될 수 있다", "~할 가능성이 있다", "~가능성이 높다", "~로 풀이된다".
- **대시(—, –)로 문장을 잇지 않는다.** AI가 쓴 티가 가장 크게 나는 지점이다.
  부연이 필요하면 문장을 끊거나 괄호를 쓴다. 값이 없는 칸은 가운뎃점(`·`).
- **브리핑은 전부 개조식.** 본문은 불렛 3~5개(각 30자 이내), 보조 항목은 한 줄 명사구.
  **온점을 찍지 않는다.** 문장체는 UI 안내 문구에서만 쓴다.
- 확실하지 않으면 단정하지 않되, 애매하게 뭉개지도 않는다. "예상 부합 / 상회 / 하회"처럼 **기준 대비**로 말한다.
- 없는 연결고리를 지어내지 않는다. 투자 함의는 실제로 있을 때만 쓴다.

## 6. 작업 방식

- **ROADMAP.md의 단계 순서를 엄수한다.** 한 번에 한 단계만. 다음 단계 기능을 미리 만들지 않는다 — 빈 폴더나 TODO 스텁도 만들지 않는다.
- **현재 단계: 3단계(데일리 브리핑) 진행 중.** 마지막 작업일 2026-08-26.
  - 배포 https://study-phi-ten.vercel.app · GitHub hyungyucreator/study · Supabase ref `jmxlffgwlkmgnavzacqh`
  - **로컬 실행:** `npm run dev` → http://localhost:3000 (dev 스크립트가 `-H ::1`로 고정돼 있음)

  **완료**
  - 1·2단계 전부: 수동 자산 CRUD, KIS 연동, 자산군 분류, 시세 갱신, 현황 대시보드(홈)
  - 3단계: RSS 수집(12피드) → 매크로 수집(FRED·ECOS) → 코드 선별(IDF 클러스터링) → 브리핑 생성(Sonnet + 캐싱) → 브리핑 뷰
  - **브리핑 구조**: 국내 경제 / 국제 경제 / 국내 정치·사회 / 국제 정치·사회 4섹션, 각 4~5건, 전부 개조식 불렛
  - **이슈 스레드**: 브리프(무엇인가/지금까지/다음 분기점), 진행·주시·종결 상태, `/threads` 목록, `/thread/[id]` 타임라인
  - **개념 카드**: 용어 점선 밑줄 → 툴팁 설명 → 단어장 저장, `/glossary`
  - **자산군 종합**: 브리핑 끝에 한 블록. 7개 코드(국내/해외 주식·채권, 원자재, 원화, 달러), 근거 기사 포함
  - **디자인**: 명조(제목)+Pretendard(본문) 2서체, 타이포 8단계 토큰, 무채색 4단계 + 방향색(적/청)

  **다음 작업 (여기서 시작)**
  1. **함의 채점** — `briefings.asset_outlook`의 방향을 1주일 뒤 `raw_market` 실제 움직임과 대조.
     **1주일치가 쌓여야 화면이 비지 않는다. 오늘 기준 브리핑 2일치뿐이므로 며칠 더 기다릴 것.**
  2. **이슈 병합** — "미 장기금리 상승과 재정건전성"과 "미 재정건전성과 디베이스먼트 트레이드"가
     같은 흐름인데 갈라져 있다. `threads.ts`의 `SAME_THREAD` 0.22로도 부족. 며칠 쌓인 뒤 병합 후보 탐색.
  3. **관련 기사 백필 정확도** — 제목 유사도에만 의존해 과거 기사를 많이 놓친다.
  4. **3단계 잔여**: Cron 배선 완료(`/api/cron/daily`, 06:30 KST, `cron_runs` 기록 + 브리핑 화면 배너. 8/26 로컬 실전 검증 완료).
     남은 것: **Vercel에 환경변수 전부 등록(사용자 작업, 이게 없으면 크론이 매일 401)**,
     텔레그램 장애 알림(봇 토큰 발급 필요), 배치 API는 보류(ROADMAP 참고).

  **열린 질문 (2026-08-26 오후, 사용자 제기 — 다음 세션에서 논의)**
  - 홈 지표 4개: 왜 중요한지 화면이 설명하지 않는다. 숫자만으로 충분한가, 1년 추이(스파크라인)가 필요한가
  - 이슈 목록: 16개 전부 "진행 중 · 1일째 · 전개 1회". 전개가 이어져야 진행 중이라 부를 수 있지 않나.
    실제 시작 시점(추적 시작 이전)을 어떻게 다룰 것인가
  - 디자인: 여전히 단조롭다. 중요도 표시가 없고 항목들이 전부 같은 무게. 톱기사·위계 장치 필요

  **DB 상태 (2026-08-26)**
  - 마이그레이션 **0001~0010 전부 적용 완료**
  - raw_news 1181 · raw_market 118 · briefings 2(8/25는 옛 구조라 이슈·용어 없음, 8/26이 정본)
  - briefing_news 30 · threads 16(전부 브리프 있음, 종결 0) · thread_news 22 · concept_cards 8 · user_cards 1
  - holdings 9(전부 KRW) · symbol_map 9 · portfolio_snapshots 2

  **미검증**
  - 모바일 실기기. 브라우저 창을 줄여도 뷰포트가 1920으로 잡혀 확인 못 함. 브레이크포인트는 넣어둠
  - 해외주식 잔고 매핑과 해외 시세·원화 환산 경로 (USD 보유 0건)
  - 이슈 종결 판정 (아직 종결된 이슈가 없어 실물 확인 못 함)

  **운영 메모**
  - 브리핑 재생성: `/api/cron/generate-briefing?force=1` (약 $0.12). `?dry=1`은 저장 없이 생성
  - 선별만 확인: `/api/cron/preview-selection`
  - 전부 `Authorization: Bearer $CRON_SECRET` 필요. `.env.local`에 있음
  - **정규식은 python heredoc으로 쓰지 말 것.** ``가 백스페이스 문자로 박혀 조용히 실패한다. Write/Edit 도구를 쓸 것

- 문서와 다르게 하고 싶은 부분이 생기면 임의로 바꾸지 말고 **먼저 사용자에게 이유와 함께 묻는다.**
- 유의미한 변경마다 `CHANGELOG.md`에 **"날짜 — 변경 — 이유"** 형식으로 기록한다.
- 사용자가 직접 해야 하는 외부 작업(Supabase 프로젝트 생성, 키 발급 등)은 단계별로 정확히 안내한다.

## 7. 기술 메모

- Next.js 16 (App Router) + Tailwind v4 + TypeScript, 배포 Vercel.
  - **Next.js 16은 학습 데이터와 다르다.** 코드를 쓰기 전 `node_modules/next/dist/docs/`의 해당 문서를 읽는다 (`AGENTS.md` 참고).
  - 미들웨어는 `src/proxy.ts` (16부터 middleware → proxy로 개명).
- Supabase(Postgres + Auth + RLS). 로그인은 Google OAuth.
- DB 스키마는 `supabase/migrations/*.sql`. 스키마 변경은 새 마이그레이션 파일로 추가하고, 기존 파일을 수정하지 않는다.
- 금액·수량은 `numeric` (부동소수점 금지). 시각은 전부 `timestamptz`.
