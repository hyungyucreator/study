# ROADMAP.md

> **작업 순서를 엄수한다** — 각 단계가 이전 단계 위에 쌓이는 구조라 순서를 어기면 품질이 무너진다.
> Claude Code에는 한 번에 한 단계씩만 지시할 것.
>
> 2026-08-25 스코프 축소: 제품을 **학습 + 현황 트래킹**으로 재정의했다.
> 의사결정 기능(목표 비중·리밸런싱·카탈로그)은 "추후 검토"로 내렸다 (PRODUCT.md §6).

## 빌드 순서

### 1. 뼈대 — 완료
- [x] Next.js + Tailwind + Supabase 셋업, Vercel 배포 파이프라인
- [x] Supabase Auth(Google) + RLS
- [x] DB 스키마 생성 (ARCHITECTURE.md §5)
- [x] Pretendard 적용, 디자인 토큰 세팅

### 2. 포트폴리오 트래킹 (데이터 파이프 검증) — 진행 중
- [x] 수동 자산 입력 CRUD
- [x] KIS API 연동: 토큰 관리 → 잔고조회 → holdings 동기화
- [x] 자산군 분류 (실질 노출 기준, `symbol_map` 티커 매핑)
- [x] 시세 갱신 (KIS 현재가 + 야후, 폴백 포함) + 환율(원화 환산)
- [x] 현황 대시보드: 홈(`/`)에 총자산·손익·자산군 비중, `/holdings`에 종목 테이블
      - 일별 스냅샷(`portfolio_snapshots`) 적재 시작. 추이 그래프는 데이터가 쌓인 뒤 붙인다

### 3. 데일리 브리핑 파이프라인 (제품의 심장 — 시간 제일 많이 쓸 것)
- [x] RSS 수집 → raw_news 저장 (12개 피드, `/api/cron/collect-news`)
- [x] 매크로 수집 (FRED, ECOS) → 온도 4지표 (`/api/cron/collect-macro`)
- [ ] 코드 레벨 뉴스 사전 필터링 (1부/2부 카테고리별, 합산 20건 내외)
- [ ] 브리핑 생성 (Sonnet + 프롬프트 캐싱 + 배치 제출/회수/폴백)
- [ ] 헬스체크 + 텔레그램 장애 알림 + 멱등 재시도
- [ ] 브리핑 뷰 + 아카이브
- [ ] **프롬프트 튜닝 반복** — 서프라이즈 판정·자산군 함의 품질이 관건.
      여기 품질이 안 나오면 다음 단계로 넘어가지 말 것

### 4. 개념 카드
- [ ] 용어 감지 → 카드 생성(Haiku, 1회) → 영구 저장 → 브리핑 내 탭 연결
- [ ] 단어장 (유저별 저장)

### 5. 이벤트 캘린더 + 트리거 브리핑
- [ ] events_calendar 구축 (FRED 릴리즈 + 수동 보완)
- [ ] 발표 후 수집 → 즉시 호출 미니 브리핑 → 발행 (30분 내)
- [ ] 고정 말미 문구 포함

### 6. 알림 (PWA Push)
- [ ] PWA 셋업 (manifest, service worker, 홈화면 추가)
- [ ] 브리핑 발행 푸시
- [ ] 급변 알림 (보유 자산군 ±2% + 원인 한 줄)

### 7. 위클리 딥다이브
- [ ] 배치 생성, 출처 링크 필수
- [ ] **"상반된 두 시각" 섹션** (에코 챔버 방지)

## 완성 후 점검 항목

- [ ] 브리핑을 2주 연속 매일 읽었는가 (안 읽었으면 기능 추가 대신 브리핑 품질 수정)
- [ ] 이 도구 사용 후 매매 빈도가 늘지 않았는가 (늘었으면 설계 원칙 위반 — 원인 제거)
- [ ] 파이프라인 무장애 연속 일수 추적

## 추후 검토 (지금 안 함)

**판단력이 붙은 뒤 재논의** (PRODUCT.md §6 — 삭제가 아니라 보류):
- 목표 비중 설정
- 목표 vs 현재 비중 막대
- 리밸런싱 주문서 (괴리 감지 → 수량 계산 → 복사 → 완료 감지)
- 매매 시점 국면 스냅샷 자동 기록
- ETF 카탈로그 (코어/틸트 2층, 국면 태그, 브리핑 역방향 링크)
- 버킷(core/tilt/experiment) UX

**기타**:
- 지인 초대 오픈 (RLS 이미 준비됨)
- 온도 지표 확장 (CAPE, 장단기 금리차 — 딥다이브에서 다룬 뒤)
- 다크모드
