# CHANGELOG.md

> 모든 유의미한 변경은 여기에 기록한다. 형식: 날짜 — 변경 내용 — 이유.

## [Unreleased]

### 2026-08-26 — 이슈 스레드와 개념 카드: 브리핑에 기억과 설명을 붙이다

"나열식이라 다른 프로덕트와 차별점이 없다"는 지적에서 시작했다. 코드로 확인해보니 맞았다. `briefingWindow()`는 시각 커트라인일 뿐이고 **어제 브리핑 내용이 오늘 모델에 한 글자도 전달되지 않았다.** 매일 20건을 읽어도 서로 연결되지 않으니 소비만 하고 축적이 없었다. 구조적으로 RSS 리더와 같았다.

**차별점은 더 좋은 요약이 아니라 시간에 있다.** 하루치 요약은 누구나 만들지만, 30일이 이어지고 그 이어짐이 머릿속에 남는 것은 구조를 가진 제품만 한다.

**이슈 스레드**

- **`0009_threads_and_terms.sql`**: `threads`, `thread_news` 공용 테이블. `briefing_news.thread_id`, `.terms`. `concept_cards.summary`.
- **판정은 코드와 모델을 섞는다.** 코드가 진행 중인 이슈 목록을 프롬프트에 넣고, 모델이 각 항목을 배정하고, 코드가 파편화를 막는다. *이유: 코드만으로는 안 된다. "캐나다산 철강 관세 25%"와 "협상단 철수"는 같은 이슈지만 공유 토큰이 "캐나다" 하나뿐이라 제목 유사도로 안 묶인다. 스토리 전개를 잇는 데는 의미 이해가 필요하다.*
- **이슈 제목은 사건이 아니라 흐름을 가리킨다.** "협상단 철수"가 아니라 "미국 통상 압박". 몇 주 뒤 후속 기사가 나와도 같은 제목 아래 묶여야 한다.
- **새 제목이 기존 이슈와 겹치면 병합한다.** `select.ts`의 IDF 지문을 재사용한다. 임계값 0.28에서는 "미 장기금리 상승과 재정건전성"과 "미 재정건전성과 디베이스먼트 트레이드"가 따로 만들어져 0.22로 낮췄다.
- **새 이슈에는 과거 `raw_news`를 붙여 타임라인의 시작점을 만든다.** 오늘 만든 이슈에 오늘 하나뿐이면 가치가 안 보인다. 매칭 기준은 모델이 새로 쓴 헤드라인이 아니라 **원문 기사 제목**을 쓴다. 어휘가 달라 헤드라인으로는 걸리지 않는다.
- 화면: 브리핑 상단 "오늘 이어진 이슈", 항목마다 이슈 행, `/thread/[id]` 타임라인.

**개념 카드와 단어장**

- 용어 감지는 **모델이 브리핑 생성 시 항목별 0~3개**를 함께 낸다. 문맥을 알기 때문에 "바이백"이 국채 바이백인지 자사주 바이백인지 구분한다. 별도 호출이 없다.
- 카드는 **Sonnet으로 1회 생성 후 영구 저장**한다. ARCHITECTURE §4-1의 "개념 카드는 Haiku"를 개정했다. *이유: 재생성이 없어 하루 몇 건뿐이라 비용 차이가 미미하고, 초보자에게 개념을 정확히 설명하는 일은 요약보다 어렵다. 값싼 모델로 틀린 설명을 영구 저장하는 쪽이 더 비싸다.*
- 화면: 본문과 보조 항목의 용어에 점선 밑줄, 눌러서 설명, 단어장에 저장. `/glossary`에 누적.
- 카드 생성이 실패해도 브리핑을 잃지 않는다. 밑줄이 안 그어질 뿐이다.

**중간에 잡은 것**

- **첫 실행에서 20건 중 8건에 이슈가 안 붙었다.** 스키마가 `id`와 `new_title` 둘 다 null을 허용해 "이슈 없음"이 유효한 출력이 됐다. `new_title`을 필수로 바꾸고, 단발성 사건도 한 단계 위의 흐름으로 이름을 붙이게 했다. *이유: 오늘 단발이어도 몇 주 뒤 이어질 수 있다. 비워두면 그 기사는 영영 아무것과도 연결되지 않는다. 화면은 이미 전개 2회 이상만 보여주므로 목록이 오염되지 않는다.* **결과: 20건 전부 배정, 그중 12건이 기존 이슈에 연결.**
- **전개 횟수가 재생성마다 중복 계산됐다.** 더하기로 하면 `force`로 다시 만들 때마다 같은 날이 또 세어져 하루 만에 "전개 5회"가 된다. `briefing_news`에서 세어 다시 쓰도록 바꿨다. 실제 기록이 정본이다.
- "오늘 이어진 이슈"를 전개 2회 이상으로만 걸면 첫날에는 아무것도 안 보인다. **오늘 여러 건이 붙은 이슈**도 포함하도록 넓혔다.

**결정**

- **투자 조언은 계속 금지.** 사후 검증(브리핑이 말한 방향과 실제 움직임 대조)까지만 허용한다. *이유: 조언은 이미 넘치고, 받기 시작하면 스스로 읽는 능력이 자라지 않는다. 검증은 조언과 반대로 도구를 덜 권위적으로 만든다.*
- **다크모드는 만들지 않는다.** "추후 검토"가 아니라 범위에서 뺐다.
- 함의 채점은 다음 차례. 최소 1주일치가 쌓여야 화면이 비지 않는다.

### 2026-08-26 — 브리핑 4섹션 개편, 개조식 전환, 두 열 레이아웃

**구조**

- **브리핑을 4섹션으로.** 국내 경제 / 국제 경제 / 국내 정치·사회 / 국제 정치·사회. "1부", "2부" 명칭은 없앴다. *이유: 축이 하나뿐이라 국내와 국제가 소구분으로 밀려 있었다. 실측해보니 후보는 이미 4분면 5:5:5:5로 균형이 잡혀 있었고, 문제는 모델이 각 부에서 5건만 골라 최종 10건이라는 점이었다.*
- **출력 스키마를 섹션별 4개 배열로.** 균형을 프롬프트 부탁이 아니라 구조로 강제한다. 각 섹션 4~5건, 총 16~20건. 후보는 섹션당 8묶음.
- **`0008_briefing_sections.sql`**: `points`(개조식 불렛 배열), `section` 컬럼 추가. 기존 행은 part/region으로 섹션을 역산해 채웠다.

**개조식 전환**

- `fact: string`을 `points: string[]`로 바꿨다. 각 30자 이내, 온점 없음. 예상 대비·맥락·지켜볼 것·자산군 함의도 한 줄 명사구. *이유: 읽는 사람은 훑어본다. 문단을 다 읽어야 뜻이 통하는 글은 매일 읽히지 않는다.*
- 모델이 온점을 찍어도 코드가 떼어낸다. 프롬프트로만 강제되지 않는다.
- 검증: 불렛 65개 중 온점 0개, 30자 초과 0개, 대시 0건.

**선별 품질**

- 지역 행정 감점(`○○시,` + 선정·지원·개최), 기업 홍보 감점(출시·협약·수주 + 단독 보도), 복수 매체 가중치 상향(×3 → ×4). *결과: 어제 상위를 차지하던 인천 재건축, 농어촌 기본소득, 대구 창업도시가 전부 빠졌다.*
- 국내/국제 판정 보강: 광역 지자체명 17개 추가, 양자 외교(`한-○○`, `한미`) 패턴은 국제로. *결과: "한-쿠웨이트 외교장관회담"이 국내에서 국제로, "경북 경산서 중국인 유학생 실종"이 국제에서 국내로 바로잡혔다.*

**레이아웃**

- **국내와 국제를 두 열로 나란히.** 4분면으로 나눠놓고 세로로 일렬로 늘어놓으면 축이 화면에서 사라진다. *이 지적을 받고서야 알았다. 구조는 맞았는데 레이아웃이 그 구조를 배신하고 있었다.*
- **1차 축을 2차 축보다 크게.** 국내/국제가 `title`, 경제/정치·사회가 `subhead`. 처음에는 뒤집혀 있어 "경제"가 "국내"보다 컸다.
- **컨테이너 71rem → 88rem.** 1920px 화면에서 콘텐츠가 65%만 차지해 좌우가 심하게 비었다.
- 사이드바, 목차, 읽는 시간을 제거했다. 시장 지표는 홈 대시보드에만 둔다.

**타이포**

- 라벨 11px → 13px, 자간 0.09em → 0.04em. 보조 문장 14px → 15px. lead 26px → 23px. *이유: 대비를 "작은 쪽 축소"로만 만들면 읽기가 나빠진다. 한글은 같은 px에서 라틴보다 작아 보인다.*

**되돌린 것**

- **브리핑의 "내 비중 N%" 배지를 제거했다.** 어제 넣은 기능이다. *이유: 브리핑은 세상을 읽는 화면이고 포트폴리오는 내 숫자를 보는 화면이다. 섞으면 둘 다 흐려진다는 사용자 판단.* PRODUCT.md §4-A의 "내 포트폴리오 알림" 조항도 함께 삭제했다.

**중간 기록**

- 정규식을 파이썬 heredoc으로 쓰다 ``가 백스페이스 문자로 박히는 실수를 또 했다. 이전에 메모까지 남긴 실수다. 소스 전체 제어문자 0건을 확인했다.
- "금값 18% 급등"이 눈에 걸려 원문을 확인했더니 리드문에 그대로 있는 표현이었다. 모델이 지어낸 게 아니다. 리드가 기간을 밝히지 않으면 브리핑도 밝힐 수 없다. 기사 전문을 수집하지 않기로 한 설계의 대가다.

### 2026-08-25 — 2차 개편: 서체 이원화, 방향색, 편집 위계

1차 개편은 규칙을 지켰을 뿐 여전히 단조롭다는 사용자 지적. 다시 보니 문제가 색이 아니라 세 군데에 있었다. 톤 단계가 둘뿐이라 위계를 크기로만 만들어야 했고, 모든 기사가 같은 크기라 편집의 흔적이 없었으며, 방향이 있는 숫자에까지 색을 안 쓰고 있었다.

- **서체 두 벌로 나눔.** Noto Serif KR을 브리핑 제목·마스트헤드·섹션 제목에 도입(`next/font/google`, 서브셋 248개라 `preload: false`). 본문과 숫자는 Pretendard 유지. *이유: 이 화면이 관리자 도구가 아니라 출판물이라는 신호를 주는 가장 큰 장치다. 본문까지 명조로 하면 매일 10분 읽는 글의 화면 가독성이 떨어진다.* DESIGN.md §3 "Pretendard 단일"을 개정했다.
- **바탕을 순백에서 뺐다.** `#FFFFFF` → `#FCFCFA`, 면 `#F5F4F1`. *이유: 순백은 "아직 디자인하지 않음"으로 읽힌다. FT의 살몬, Monocle의 종이질감이 하는 일을 무채색 안에서 한다.*
- **무채색을 2단계에서 4단계로** (`ink` / `fg` / `muted` / `faint`), 선을 2단계로 (`line` / `line-strong`). *이유: 단계가 둘뿐이면 위계를 색이 아니라 크기로만 만들어야 해서 화면이 평평해진다.*
- **타이포 스케일을 벌리고 `lead`를 신설.** display 34→44, title 24→28, heading 19→21, body 16→17. *이유: 섹션 제목과 본문이 3px 차이면 그건 위계가 아니다.*
- **적/청을 "손익"에서 "시장 방향"으로 확장.** 지표 변화값(`VIX +0.72`)과 자산군 상방·하방에 적용. *이유: 규칙을 어기지 않으면서 화면에 의미 있는 색이 들어온다. 지금까지 색이 없던 건 절제가 아니라 그냥 빠뜨린 것이었다.* 자산군마다 색을 주는 것은 여전히 금지한다. 색이 두 가지를 뜻하면 방향으로 안 읽힌다.
- **편집 위계 도입.** 각 부의 첫 기사를 `text-lead`로 크게 두고 라벨을 걷어냈다. *이유: 신문 1면은 톱기사가 압도적으로 크다. 1번과 5번이 똑같으면 무엇이 중요한지 화면이 말해주지 않는다. 문단마다 이름표가 붙으면 기사가 아니라 서류로 읽힌다.*
- **내 보유 자산군 표시** (PRODUCT.md §4-A-4). 자산군 함의에 "내 비중 14.8%" 배지. *이유: 브리핑이 남 얘기에서 내 얘기가 되는 지점이고, 동시에 화면의 자연스러운 시각적 앵커가 된다.*
- **홈에 시장 상황 4지표를 가로로 추가.** *이유: 브리핑은 출판물, 홈은 계기판이라는 성격 분리. 오른쪽이 비어 보이던 문제도 함께 해결된다.*
- 읽는 시간을 기사 수 기반 추정으로 바꿨다. 고정 "10분"은 기사 수와 무관해 거짓말이 된다.

### 2026-08-25 — 디자인 전면 개편, 대시 제거, 개조식 도입

DESIGN.md를 다시 대조한 결과 지킨 것보다 못 지킨 것이 많았다. 타이포가 임의값으로 흩어져 스케일 체계가 없었고, 컨테이너 폭이 화면마다 다섯 종류였으며, "모바일 우선"과 "신문처럼"은 사실상 지켜지지 않았다.

**디자인 토큰** (`src/app/globals.css`)

- **타이포 스케일 7단계 토큰화** (`display` / `title` / `heading` / `subhead` / `body` / `small` / `label`). `text-[17px]` 같은 임의값을 전면 금지하고 `src/app` 전체에서 0건으로 만들었다. *이유: 임의값이 흩어지면 위계가 무너진다. 섹션 제목과 본문 차이가 3px밖에 안 나던 문제가 여기서 왔다.*
- **면 톤 `--color-surface: #FAFAFA` 1단계 추가.** 카드와 그림자를 대신해 블록을 묶는 수단이다. 무채색은 유지.
- **컨테이너 폭 3종으로 통일** (`prose` 42rem / `page` 68rem / `form` 26rem). 기존에는 `max-w-sm` `2xl` `4xl` `xl` `[46rem]` 다섯 종류가 섞여 있었다.
- `label` 유틸 추가. 라벨의 크기·자간·색을 한 곳에서 관리한다.

**전역 셸**

- 라우트 그룹 `src/app/(app)/`으로 인증 화면을 묶고 마스트헤드를 공용화했다. URL은 그대로다. *이유: 페이지마다 헤더를 따로 그리니 폭도 여백도 제각각이었고, 페이지 간 이동 수단이 "홈" 텍스트 링크뿐이었다.*
- 현재 위치는 굵기와 밑줄로만 표시한다. 색으로 상태를 만들지 않는다.

**브리핑 화면**

- **데스크탑 2단.** 본문은 한 컬럼(`prose`)을 유지하고 `lg` 이상에서만 시장 상황과 목차를 사이드로 뺐다. 모바일에서는 시장 상황이 본문 위로 접힌다. DESIGN.md §4의 "한 컬럼"을 본문 기준으로 해석하고 문서에 명시했다.
- 마스트헤드(날짜 `display` + 굵은 선)로 신문처럼 시작을 알린다.
- 시장 상황을 `surface` 면으로 묶었다.
- **자산군 함의를 좌측 2px 세로선으로 분리.** 이 제품이 내놓는 핵심 산출물인데 다른 항목과 같은 밀도로 묻혀 있었다.
- 라벨을 좌측 열에서 내용 위로 올렸다. 좁은 화면에서 좌측 라벨 열은 폭만 잡아먹는다.

**홈 대시보드**

- 총자산을 `display`로 키워 숫자가 주인공이 되게 했다.
- **오늘 브리핑 헤드라인 4건 추가.** 대시보드가 대시보드다워지는 지점이다. `loadBriefing`을 그대로 재사용한다.

**보유자산**

- **모바일은 행 단위 스택, 데스크탑은 표.** 기존에는 `min-w-[640px]` 표라 모바일에서 가로 스크롤이 났다. 모바일 우선 원칙 위반이었다.
- 표에 `table-fixed`와 열 폭 지정. *이유: 자산군마다 표가 따로라 auto 레이아웃에서 표마다 열 위치가 어긋났다. 자릿수 비교가 안 되면 tabular-nums를 쓰는 의미가 없다.*

**대시 제거와 개조식**

- 사용자에게 보이는 대시 6곳을 전부 없앴다: 자산군 함의, 시장 상황 보조문구, 마크다운 렌더, 브라우저 탭 제목 3곳, 빈 값 표시(`·`로 대체), 금리 범위(`3.50~3.75%`).
- 프롬프트에 **대시 금지 규칙**과 **개조식 규칙**을 넣었다. 자산군 함의 note는 한 줄 40자 이내, "~함"이나 명사구 종결. 사실·서프라이즈·맥락·전망은 문장체 유지. *이유: 대시가 AI 냄새의 주된 원인이라는 사용자 지적. 다만 전면 개조식은 맥락을 죽여 "이해 도구"라는 제품 목표와 충돌한다.*
- 검증: 재생성 결과 모델 출력 내 대시 0건, 자산군 note 8건 전부 45자 이내 개조식.

**중간에 잡은 버그**

- Tailwind v4의 기본 border 색이 `currentColor`라 `border-t`에 `border-line`을 빼먹은 곳이 검은 선으로 그려지고 있었다. DESIGN.md에 명시했다.

**미검증**: 모바일 실기기. 브라우저 창을 줄여도 스크린샷이 1568px로 고정 캡처돼 확인하지 못했다. 코드 레벨로는 가로 스크롤 유발 요소(고정 폭, `min-w`, `whitespace-nowrap`)를 전부 점검해 안전을 확인했다.

### 2026-08-25 — "오늘의 온도" → "시장 상황", 브리핑 화면 1차 디자인

- **용어 변경: "오늘의 온도" → "시장 상황".** *이유: 온도라는 비유가 어색하다는 사용자 지적. 네 지표가 가리키는 것을 그대로 부르는 편이 낫다.* PRODUCT.md §4-A-1, CLAUDE.md, 프롬프트·화면·마크다운 전부 반영. DB 컬럼 `briefings.temperature_json`은 그대로 둔다 — 이름을 바꾸려면 마이그레이션이 필요한데 얻는 게 없다.
- **브리핑 화면을 실제로 띄워 보고 다듬었다.** 그전까지는 로그인이 필요해 렌더 결과를 한 번도 확인하지 않은 상태였다.
  - 본문 폭 46rem, 여백 리듬 정리. 지표는 22px 굵게 + 보조선 13px.
  - **자산군 함의를 세로선(1px)으로 분리해 도드라지게** 했다. 이 제품이 내놓는 핵심 산출물인데 다른 항목과 같은 밀도로 묻혀 있었다.
  - 항목 번호를 **부 전체에서 연속**으로 바꿈. 국내/해외 그룹마다 1로 돌아가면 "1부 3번"으로 지칭할 수 없다.
  - 국내/해외 소제목을 굵게 + 아래 실선으로 분명하게. 이전에는 muted 13px이라 거의 안 보였다.
  - **출처에 매체명 표시** ("매일경제 증권 원문 보기"). 매체를 보여주는 것이 해석 검증의 첫 단서다. `implication_json.source_name`으로 저장(마이그레이션 불필요).
  - 미 기준금리 보조문구를 "2025-12-10 인하 후 동결"로 압축. 좁은 칸에서 두 줄로 깨졌다.
- **미검증**: 모바일 폭. 브레이크포인트는 넣었지만 실제 기기 폭에서 확인하지 못했다.

### 2026-08-25 — 국내/해외 축 도입 (PRODUCT.md §4-A 개정)

- **문제**: 경제/비경제는 1부·2부로 갈려 있었지만 국내/해외가 각 부 안에서 뒤섞여 있었다. 사용자 지적. 실제로 그날 1부는 국내 2·해외 3이 섞인 채 순서 없이 나열됐고, 선별 단계에서 국내/해외 균형을 아예 보지 않았다.
- **`src/lib/news/region.ts`** — 판정 기준은 **매체 국적이 아니라 사건의 무대**. 영문 매체와 `world` 분류는 해외, 그 밖에는 한국 무대 앵커(코스피·국회·금통위·국내 대기업 등)가 있으면 국내, 없고 해외 앵커(美·中·트럼프·연준 등)만 있으면 해외. *이유: 연합뉴스 국제면 기사를 "국내"로 두면 축이 무의미해진다. 반대로 "美반도체 찬바람에 코스피 하락"은 무대가 한국 증시라 국내다.*
- **선별에 국내/해외 균형 확보** — 각 부에서 양쪽을 절반씩 먼저 채운 뒤 점수로 마저 채운다. *결과: 1부 국내 5·해외 5, 2부 국내 5·해외 5로 후보가 균형을 이룸.*
- **프롬프트에 후보별 [국내]/[해외] 표시**와 한쪽 쏠림 금지 규칙 추가. 화면·마크다운도 각 부를 국내/해외 소제목으로 나눔.
- 잡음 태그 보강: `[MK시그널]`(개별 종목 매수신호 기사 — 특히 실으면 안 된다), `[연합뉴스 이 시각 헤드라인]`, `[차트]`, `[포착]`.
- **프롬프트 2차 튜닝** — 이번 결과에서 나온 문제:
  - 리드문에 없는 고유명사를 기억으로 채워 넣는 환각(재무장관 이름을 다른 인물과 혼동). "후보에 없는 고유명사 금지" 규칙 추가.
  - "가능성이 높지만" 형태로 뭉개는 표현 재발. 금지 목록 확장.
- **`preview-selection` 라우트도 같은 구간 버그가 있었다** — `briefingWindow()`에 날짜를 넘기지 않아 후보가 0건으로 나왔다. 생성 라우트와 같은 구간을 보도록 수정.
- 검증: 재생성 결과 1부 국내 2·해외 3, 2부 국내 3·국제 2. 뭉개는 표현 0건, 지어낸 링크 0건.

### 2026-08-25 — 브리핑 재생성 시 뉴스 구간이 비던 문제

- **`briefingWindow(forDate)`** — 기준점을 "가장 최근 브리핑"에서 "해당 날짜 **이전** 브리핑"으로 바꿈. *이유: 오늘 브리핑을 재생성하면 방금 만든 자기 자신이 기준이 돼 구간이 몇 분으로 줄고 후보가 0건이 됐다. 일반 실행에도 이쪽이 맞다 — 오늘 것을 기준 삼을 이유가 없다.*
- **Anthropic 호출에 3회 재시도 (429·5xx·네트워크 오류, 지수 백오프).** *이유: 외부 호출 한 번이 튀면 그날 브리핑을 통째로 잃는다. 400·401처럼 다시 걸어도 같은 오류는 즉시 중단한다.*

### 2026-08-25 — 브리핑 뷰 + 아카이브

- **`/briefing`(최신), `/briefing/[date]`(지난 날짜)** — 오늘의 온도 4지표, 1부·2부 3단 구조, 원문 링크, 하단 아카이브 목록.
- **저장된 마크다운을 파싱하지 않고 `briefing_news`를 직접 읽어 렌더한다.** *이유: 마크다운 렌더러 의존성이 없고, 3단 구조를 DESIGN 규칙(카드 없이 여백과 1px 선, 위계는 크기·굵기로만)대로 정확히 그릴 수 있다. `body_md`는 아카이브·알림 채널용으로 남긴다.*
- **`0007_briefing_headline.sql`** — `briefing_news.headline` 추가. *이유: 모델이 이미 headline을 만드는데 저장할 자리가 없어 버려지고 있었고, 화면에서는 fact 첫 문장을 잘라 쓸 수밖에 없었다.*
- 홈에 브리핑 링크 추가.

### 2026-08-25 — 브리핑 생성 (Sonnet + 프롬프트 캐싱)

- **`src/lib/briefing/`** — 프롬프트, 구조화 출력 스키마, Anthropic 호출, 마크다운 렌더, 저장. 라우트 `/api/cron/generate-briefing` (`?dry=1` 저장 없이 생성, `?force=1` 재생성).
- **모델에게 마크다운을 통째로 쓰게 하지 않는다.** 도구 호출(`submit_briefing`)로 구조만 받고 본문은 코드가 조립한다. *이유: 형식·제목 수준·순서가 프롬프트만으로는 매일 흔들린다. 구조로 받아야 `briefing_news`에 3단 구조 그대로 저장되고 나중에 화면에서 재구성할 수 있다.*
- **시스템 프롬프트는 매일 글자까지 동일하게 두고 `cache_control`을 건다.** 날짜·뉴스·지표는 전부 user 메시지로 분리. *이유: 캐싱은 접두사 일치로 동작한다(ARCHITECTURE §4-2).* **검증: 2회차 호출에서 캐시 읽기 2,813토큰 확인.**
- **출처 링크 검증.** 모델이 낸 url이 후보 목록에 없으면 그 항목을 통째로 버린다. *이유: 출처는 해석을 검증할 유일한 경로다. 틀린 링크를 싣느니 항목을 빼는 게 낫다.* (실측 결과 지어낸 링크 0건)
- **매체명은 모델 출력을 버리고 후보 값으로 덮어쓴다.** *이유: 첫 실행에서 "경향신문"을 "경향신점"으로 썼다. 이미 갖고 있는 값을 받아쓸 이유가 없다.*
- **프롬프트 1차 튜닝** — 첫 결과에서 나온 문제를 고쳤다:
  - 뭉개는 표현("~될 수 있다", "~가능성이 있다") 금지. 근거가 약하면 direction을 `unclear`로 두고 무엇을 모르는지 쓰게 함.
  - outlook이 매번 "~지켜봐야 한다"로 끝나던 것을 명사구로 바꿈.
  - context의 동어반복("~하는 절차다") 금지.
- 비용 실측: 1회 **$0.066** (입력 4,784 + 캐시읽기 2,813 + 출력 3,369토큰). 출력이 대부분을 차지한다. 배치 API 적용 시 절반.
- 검증: 1부 5건 / 2부 5건 생성, `briefing_news` 10행 전부 `raw_news` 연결, 온도 4지표 스냅샷 저장.
- **아직 안 한 것: 배치 API.** 품질을 먼저 보는 게 순서라 일반 호출로 만들었다. 배치 제출/회수/폴백은 다음 작업.

### 2026-08-25 — 뉴스 사전 필터링: 670건 → 20묶음

- **`src/lib/news/select.ts`** — 사건 단위로 묶고 점수를 매겨 1부 10 / 2부 10으로 추린다. DB·네트워크를 모르는 순수 함수로 두어 입력을 손으로 넣고 튜닝할 수 있게 했다. 확인용 라우트 `/api/cron/preview-selection` (모델 호출 없음). *이유: ROADMAP 3단계가 "프롬프트 튜닝 반복"을 요구하는데 모델에 무엇이 들어가는지 못 보면 튜닝이 불가능.*
- **가장 강한 신호는 다수 매체 동시 보도** (ARCHITECTURE §2 뉴스 교차 확인). 같은 사건을 여러 매체가 쓰면 그날 중요한 사건이다.
- **유사도를 IDF 가중으로 바꿈.** 맨 자카드는 "하락"·"정부" 같은 흔한 조각을 고유명사와 똑같이 세서, 임계값을 낮추면 무관한 기사가 붙고 높이면 같은 사건이 갈라졌다. 그날 후보 전체의 조각 빈도로 가중하니 둘 다 해결. *결과: 당정 예산안 3매체, 이진숙 제명안 3매체가 정상 병합되면서 오병합은 사라짐.*
- **언어별로 지문을 다르게 만든다.** 한국어는 글자 2-gram, 영어는 단어 집합. 영어에 2-gram을 쓰면 "th"·"in" 같은 흔한 조각 때문에 관세 기사와 이란 기사가 한 묶음이 됐다.
- **장중 시황 중계는 규칙으로 접는다.** 통신사가 같은 장을 하루 여러 번 고쳐 쓰는데("장초반 2%대 하락" → "1~2%대 하락세" → "2~3%대 하락(종합)") 제목이 매번 달라 지문으로는 안 묶인다. "(종합)" 최종본만 남기고 나머지는 묶음에 붙인다.
- **잡음 배제**: 매체가 스스로 붙인 대괄호 태그(`[게시판]`, `[부고]`, `[특징주]` 등), 단독 보도 공지성 기사, 영문 서비스 기사("How to…", "Tuesday's big stock stories").
- **영문 키워드 추가.** 점수의 주제 가중치가 한국어 단어뿐이라 BBC·CNBC 기사는 항상 0점을 받고 있었다. 대소문자 무시 비교로 바꿈.
- **2부 카테고리 최소 보장(각 2건).** 정치 기사가 많은 날 2부가 통째로 정치가 되고 국제·테크가 밀렸다. *결과: 국제 1건 → 2건.*
- 검증: 후보 670건 → 20묶음(복수매체 5, 병합 기사 22), 분류 분포 market 6 · economy 4 · politics 4 · society 2 · tech 2 · world 2.
- **남은 튜닝 지점**: 지역 행정 기사(인천 재건축, 농어촌 기본소득)가 1부 상위에 남는다. 브리핑을 실제로 읽어보고 조정한다.

### 2026-08-25 — 매크로 수집: 오늘의 온도 4지표

- **`src/lib/macro/` + `/api/cron/collect-macro`** — 미 기준금리(FRED `DFEDTARU`/`DFEDTARL`), 미 10년물(`DGS10`), VIX(`VIXCLS`), 원달러(야후 실시간 + FRED `DEXKOUS` 전 영업일 종가). 한국 기준금리(ECOS `722Y001`)는 적재만 하고 화면에는 띄우지 않는다. *이유: PRODUCT §4-A-1이 온도를 4지표로 고정했고 "지표 확장은 딥다이브에서 다룬 뒤에만"이라고 못박음.*
- **해석 문구를 모델이 아니라 코드가 만든다.** VIX만 통용되는 구간 해석(15/20/30)을 쓰고, 나머지는 임의 구간 대신 **최근 1년 분포 내 위치**로 말한다("최근 1년 최고 수준" 등). *이유: 모델에 맡기면 매일 표현이 흔들리고 근거 없는 구간을 지어낸다(CLAUDE.md §5). 임의 임계값을 만드는 것도 같은 문제 — 분포 대비는 데이터가 스스로 말한다.*
- **정책금리는 전일 대비를 쓰지 않고 "마지막 변경 이후 동결"로 표기.** *이유: 매일 움직이지 않는 값에 전일 대비를 붙이면 항상 0이라 정보가 없다.*
- **FRED·ECOS 조회에 3회 재시도.** *이유: 첫 실행에서 VIX 하나가 네트워크 blip으로 통째로 빠졌다. 지표 하나를 한 번의 실패로 잃지 않게.*
- 검증: 연속 2회 실행 모두 4지표 전부 수집, 값 동일. 원달러 "최근 1년 최저 수준" 판정은 DEXKOUS 249개 관측(1,375.5~1,556.0) 중 하위 0.8%로 검산 확인.

### 2026-08-25 — 3단계 시작: API 키 확보, RSS 수집 파이프라인

- **API 키 3종 발급·검증** — FRED(미 10년물 4.74% 응답), ECOS(기준금리 2.5% 응답), Anthropic(과금 호출 성공). 전부 서버 전용, `.env.example`에 자리만 문서화. *이유: 3단계는 외부 데이터와 모델 호출이 전제. 키가 실제로 도는지 먼저 확인해야 파이프라인 디버깅과 키 문제를 섞지 않는다.*
- **RSS 수집 → `raw_news`** (`src/lib/news/`, `/api/cron/collect-news`). 12개 피드, 36시간 창, url unique로 멱등 재시도. 저장은 제목·리드문(300자 상한)·링크·발행시각뿐. *이유: ARCHITECTURE §3-3(가공 전 원본 저장) + §3-5(멱등성). 기사 전문은 저장하지 않는다(CLAUDE.md §2-3).*
- **피드 선정을 추측이 아니라 실측으로 확정.** 후보 40여 개를 찔러본 결과로 문서에 적힌 매체 일부를 교체:
  - 한국경제 — RSS가 403(봇 차단). 크롤링 우회는 하지 않는다(§2-4).
  - Reuters — 공개 RSS 폐지(404). BBC Business·CNBC로 대체.
  - 조선일보 — description이 비어 제목만 온다. 동아일보 정치로 교체(보수 성향 유지).
  - 한겨레 — pubDate가 없고 description은 이미지 태그뿐. 경향신문으로 교체(진보 성향 유지).
  *이유: 리드문·발행시각이 없으면 브리핑 재료가 안 되고 "직전 브리핑 이후"를 자를 수 없다. 성향 대조(연합=사실 축, 동아=보수, 경향=진보)는 유지.*
- **종합 피드는 기사 자체 분류를 화이트리스트로 거른다** (`Feed.itemCategory`). 경향 종합 피드 50건 중 33건만 통과. *이유: 스포츠·연예·지역 기사가 브리핑 후보에 섞이면 토큰만 쓴다.*
- **크론 라우트 인증은 `CRON_SECRET` 단일.** 값이 없으면 전부 401 — 열어두는 쪽으로 실패하지 않는다. `proxy.ts`의 공개 경로에 `/api/cron` 추가(크론은 쿠키가 없다). *이유: 이 라우트는 세션이 아니라 스스로 인증해야 한다.*
- 검증: 12개 소스 전부 정상, 685건 적재, 리드문 보유율 94~100%, 재실행 시 중복 0건(신규분만 저장).

### 2026-08-25 — 현황 대시보드, 그리고 화면 단순화

- **홈(`/`)이 대시보드가 됨** — 총자산(원화), 손익·수익률, 자산군별 비중(막대는 길이로만, 자산군에 색을 주지 않음), 기준 시각. *이유: 2단계의 목적이 "내 자산 현황 확인"인데 홈이 빈 링크 목록이었음.*
- **집계 로직을 `src/lib/portfolio.ts`로 통합** (홈·보유자산 공용). *이유: 같은 원화 환산·합계 계산이 두 화면에 복제되면 숫자가 갈라짐.*
- **`0006_portfolio_snapshots.sql`** — 일별 총자산 스냅샷(user_id, date PK). 평가액과 매입원가를 함께 남기고 자산군별 금액을 jsonb로 보관. RLS 4정책. *이유: holdings는 "지금"만 담아 추이를 그릴 수 없고, 자산 증가가 납입 때문인지 상승 때문인지 구분하려면 매입원가 시계열이 필요. 오늘부터 쌓지 않으면 과거는 영영 복구 불가.*
- **추이 그래프는 만들지 않음.** 대신 "직전 기록 대비 ±금액 · 납입 ±금액"만 한 줄. *이유: 데이터가 하루치뿐이라 그래프가 점 하나. 쌓인 뒤에 붙인다.*
- **KIS 동기화 항목의 수정 범위를 분류로 제한** — 목록의 링크는 "분류", 화면에서 자산군·ETF 여부만 수정, 삭제 없음. 수동 입력 항목만 전체 수정·삭제. *이유: KIS 항목의 수량·평단을 고쳐도 다음 동기화에 덮어써져 무의미했음. 실제로 사용자가 바꿀 수 있는 것은 분류뿐이고, 그 값은 `symbol_map`에 기억돼 유지됨.*
- **상단 요약에서 매입금액 줄 제거** (총자산 · 손익 · 수익률만). *이유: 손익과 수익률이 있으면 매입금액은 역산되는 파생값. 단순함이 우선.*

### 2026-08-25 — 시세 갱신: 평가금액·수익률·원화 환산 총액

- **시세 모듈 `src/lib/quotes/` 신설** — 국내는 KIS 현재가(`inquire-price`, TR `FHKST01010100`), 해외와 원달러 환율은 야후 파이낸스 차트 JSON. *이유: ARCHITECTURE §2가 정한 소스(국내 KIS / 해외 yfinance)를 그대로 따름. yfinance 파이썬 라이브러리가 호출하는 것과 같은 JSON 엔드포인트를 직접 쓰므로 HTML 크롤링이 아님(CLAUDE.md §2-4).*
- **환율 소스로 ECOS 대신 야후 `USDKRW=X` 사용.** *이유: ECOS는 API 키 발급이 필요하고 전일 매매기준율이라 장중 환산에 맞지 않음. ARCHITECTURE §2의 ECOS는 브리핑 매크로(기준금리·환율) 용도이며 3단계에서 별도로 붙인다. 포트폴리오 환산용 환율은 야후 하나로 충분.*
- **시세 캐시는 새 테이블 없이 기존 `raw_market`에 적재** (`kind='price'|'fx'`, `as_of`는 제공자 체결시각이 아니라 우리가 관측한 시각). 10분 이내 관측치가 있으면 재조회하지 않음. *이유: 스키마를 늘리지 않고, 동시에 ARCHITECTURE §3-4의 "시세 동결 감지"에 쓸 시계열이 자연히 쌓임. as_of를 관측 시각으로 두어야 장 마감 후에도 캐시 신선도 판단이 가능.*
- **시세를 못 받은 종목은 매입가로 메우지 않고 `—`로 두고 합계에서 제외**, 화면에 "N건 시세 미반영"을 표시. *이유: 없는 값을 평단으로 채우면 손익 0%로 보이는 착시가 생김.*
- **조회 실패 시 오래된 캐시(최대 7일)로 폴백.** *이유: 야후·KIS 한쪽이 잠깐 죽어도 화면 전체가 비지 않게.*
- **KIS 요청 공통부를 `src/lib/kis/request.ts`로 분리** (잔고조회·현재가조회 공용). *이유: 같은 헤더·에러처리·연속조회 로직이 두 벌이 되는 것을 막음. 주문 TR은 여전히 어디에도 두지 않는다.*
- **`/holdings` 화면 개편** — 총자산(원화 환산) + 매입금액 + 평가손익·수익률, 종목별 현재가·평가금액·손익, 기준 시각과 적용 환율 표시. 손익 색은 적(수익)/청(손실)만. *이유: 2단계의 목적이 "내 자산 현황을 숫자로 확인"이므로.*
- 검증: KIS 현재가는 보유 8종목(개별주·ETF 포함) 실호출로 정상 응답 확인. 야후는 `SPY`, `USDKRW=X` 응답 확인. `raw_market` 중복 적재는 `on_conflict`로 무시됨을 확인. **미검증: USD 보유 종목이 없어 해외 시세·환산 경로는 실데이터로 확인하지 못함.**

### 2026-08-25 — 세션 종료 시점 상태

- 마이그레이션 **0001~0005 전부 적용 완료.** `target_weights`·`rebalance_orders` 삭제 확인, `holdings.bucket` null 0건.
- **KIS 동기화 실제 동작 확인** — 9행(국내 8종목 + 예수금) 반영. 자산군 자동 분류 9/9 정확:
  미국30년국채·단기채권 → 채권 / 골드선물 → 원자재 / 미국달러선물 → 통화 /
  TIGER 미국S&P500 → 해외주식 / SOL 자동차TOP3·하나금융·KB금융 → 국내주식 / 예수금 → 현금.
- **0003을 재실행 가능하게 수정** (`drop policy if exists` 추가). *이유: `create policy`는 IF NOT EXISTS를 지원하지 않아 중간에 끊긴 마이그레이션을 다시 돌릴 수 없었음. 앞으로 마이그레이션은 전부 재실행 가능하게 작성한다.*
- **다음 작업:** 시세 갱신(KIS 현재가 + yfinance 폴백 + 원달러 환율) → 현황 대시보드.

### 2026-08-25 — 스코프 축소: 제품을 "학습 + 현황 트래킹"으로 재정의

**왜:** 자산군 분류·버킷 논의를 거치며 드러난 것은, 제품이 사용자보다 앞서 나가고 있었다는 점이다.
목표 비중을 정하려면 자산군별 기대수익·변동성·상관관계에 대한 감각이 먼저 필요한데,
그 근거가 없는 상태에서 목표·괴리·주문서를 붙이면 사용자는 제품이 제시한 숫자를 규칙처럼 따르게 된다.
그건 "판단은 사용자가"라는 1원칙을 형식만 지키고 실질은 어기는 것이다.
브리핑 품질(제품의 심장)에 자원을 집중하기 위해서도 지금은 범위를 좁히는 편이 낫다.

- **범위 밖으로 이동** (삭제 아님, PRODUCT.md §6 "판단력이 붙은 뒤 재논의"):
  목표 비중 설정, 목표 vs 현재 비중 막대, 리밸런싱 주문서, 매매 시점 국면 스냅샷,
  ETF 카탈로그, 버킷(core/tilt/experiment) UX.
- **유지:** 데일리·이벤트 브리핑, 개념 카드, 딥다이브, 포트폴리오 트래킹(KIS 동기화 + 수동 입력,
  총자산 추이, 자산군 비중, 종목 테이블), 급변 알림.
- **스키마:** `target_weights`·`rebalance_orders` 테이블 제거 (`0005_scope_reduction.sql`, 데이터 없음).
  `holdings.bucket`은 기본값 `core`로 남기고 UI에서 제거. *이유: 되살릴 때 마이그레이션 비용을 줄이기 위함.*
- **코드:** 버킷 라디오·"버킷 지정 필요" 블록·실험 20% 표시·`setBucket` 액션·`rememberBucket` 제거.
- PRODUCT.md(§1 재정의, §2 원칙 추가, §4-C 조회 전용으로 정리, §6 신설),
  ROADMAP.md(빌드 순서 재정렬), CLAUDE.md 반영.

### 2026-08-25 — 버킷(계좌 성격) 도입

- **`holdings.bucket` + `symbol_map.bucket` 추가** (`0004_bucket.sql`): core / tilt / experiment, null은 미지정. *이유: PRODUCT §3이 사용자를 "ETF 중심 자산배분 + 소액 개별주 실험 계좌"로 정의하는데 스키마에 구분이 없어 실험용 개별주가 자산배분 비중에 섞였다. 그러면 코어 비중이 왜곡되고 리밸런싱 주문서가 실험 종목까지 건드린다.*
- **목표 비중·리밸런싱 대상은 core + tilt만** (`countsTowardAllocation`). experiment는 비중에서 제외하고 따로 본다.
- **experiment 합계가 전체의 20% 초과 시 화면에 표시.** 차단하지 않는다. *이유: 판단은 사용자가 한다는 원칙. 다만 실험분이 조용히 불어나는 것은 본인이 알아야 한다. 현재는 환율 연동 전이라 원화 자산 매입금액 기준으로 계산하고 그 사실을 화면에 명시.*
- **버킷은 추정하지 않는다.** KIS 신규 종목은 `미지정`으로 들어오고, 목록 상단 "버킷 지정 필요" 블록에서 한 번 누르면 `symbol_map`에 기억돼 다음 동기화부터 자동 적용. *이유: 자산군은 이름으로 추정할 근거가 있지만 버킷은 사용자의 의도라 추정할 근거가 없다.*
- PRODUCT §3·§4-C, ARCHITECTURE §5 반영.

### 2026-08-25 — 자산군을 실질 노출 기준으로 변경 (문서 개정 포함)

- **자산군에서 `etf`를 빼고 `currency`(통화)를 추가** (`0003_asset_class_exposure.sql`). 새 목록: 국내주식/해외주식/채권/원자재/통화/현금/기타. ETF 여부는 `holdings.is_etf` 플래그로 분리. *이유: 기존 목록은 상품 형태(ETF)와 자산 성격(채권·원자재)이 한 층위에 섞여 있었고, 보유 ETF가 전부 'ETF'로 몰리면 "목표 비중 vs 현재 비중" 막대가 "ETF 75%"로 나와 자산배분 판단에 쓸 수 없음. PRODUCT §4-C, ARCHITECTURE §5 개정.*
- **`symbol_map` 테이블 추가** — 티커별 분류를 사실로 저장(공용 읽기, service_role 쓰기). 보유 9종목 초기 분류 시드 포함. *이유: 이름 추정에 매번 의존하지 않기 위함. 한 번 확정하면 재매수·재동기화 시 자동 재사용.*
- **분류 우선순위: `symbol_map` > 기존 holdings 값 > 이름 키워드 추정.** 키워드 규칙은 채권 → 원자재 → 통화 → 해외주식 순으로 검사. *이유: "미국30년국채"에서 '미국'이 먼저 걸리면 해외주식으로 오분류되므로 순서가 결과를 좌우함.*
- 사용자가 화면에서 분류를 고치면 서버 액션이 `symbol_map`에 기록. *이유: 같은 판단을 두 번 하지 않게 하기 위함. symbol_map은 service_role만 쓸 수 있어 서버가 대신 기록한다.*

### 2026-08-25 — 로컬 개발 서버 기동 불가 해결

- **`dev` 스크립트를 `next dev -H ::1`로 고정.** *이유: 개발 PC의 Winsock IPv4 바인딩이 손상돼 새 프로세스가 IPv4 소켓을 열 수 없음(포트 무관, Node·.NET 모두 WSAEFAULT, IPv6는 정상, 기존 리스너 39개는 멀쩡, 아웃바운드 연결도 정상). Next·프로젝트 코드 문제가 아니며 IPv6 루프백 바인딩으로 우회. Windows가 localhost를 ::1로 해석하므로 `http://localhost:3000` 접속은 그대로 동작.*
- 원인 후보였던 DLL 주입(KOSinj 등)은 배제 — `node.exe`에 주입된 비시스템 DLL이 없음. 근본 해결책은 `netsh winsock reset` + 재부팅이며, README에 기록.

### 2026-08-24 — 2단계: KIS 연동 (토큰 → 잔고조회 → holdings 동기화)

- **KIS 토큰 캐시를 DB 테이블로 (`0002_kis_token.sql`)** — RLS를 켜되 정책을 만들지 않아 service_role만 접근 가능. *이유: ARCHITECTURE §7이 토큰 캐시를 요구하는데 서버리스는 인스턴스 메모리가 유지되지 않아 매 요청 재발급 위험이 있고(KIS는 1분 1회 제한), 이 토큰은 주문 권한까지 가진 자격증명이라 브라우저 세션으로 읽히면 안 됨.*
- **`src/lib/kis/` 추가** — `token.ts`(발급·캐시·만료 10분 여유), `balance.ts`(국내 TTTC8434R / 해외 TTTS3012R, 연속조회), `sync.ts`(holdings upsert). 전부 `server-only`. *이유: 잔고조회는 읽기 전용이며, 주문 TR은 어디에도 두지 않는다(CLAUDE.md §2-1).*
- **동기화 규칙** — 사용자가 고친 `asset_class`는 다음 동기화가 덮어쓰지 않음, KIS에서 사라진 종목은 `source='kis'` 행만 삭제(수동 입력 행 보존), 보유수량 0(당일 전량매도)은 제외, 예수금(`dnca_tot_amt`)을 현금 자산으로 반영. *이유: 자동 동기화가 사용자의 수정과 수동 입력을 파괴하지 않아야 하고, 현금은 자산배분 비중 계산에 필요함.*
- **국내 ETF는 상품명 브랜드로 추정** (KODEX/TIGER/ACE/SOL 등). 실계좌 8종목으로 검증 — ETF 6건·주식 2건 정확히 분류. *이유: 잔고 응답에 상품 유형 필드가 없음. 틀리면 사용자가 고치고, 고친 값은 유지됨.*
- **`KIS_OWNER_EMAIL` 게이트 추가** — 설정 시 해당 계정만 동기화 가능. *이유: KIS 계좌는 소유자 한 명의 것이라, 지인 공유 단계에서 남의 잔고가 그 사람 포트폴리오로 들어가는 사고를 미리 막음.*

### 2026-08-24 — 2단계 시작: 수동 자산 입력 CRUD

- **`/holdings` 보유자산 화면** — 자산군별 그룹 테이블(수량·평단·매입금액 전부 tabular-nums), 추가 폼, `/holdings/[id]` 수정·삭제. *이유: ROADMAP 2단계 첫 항목이며 외부 API 키 없이 데이터 경로(RLS → 조회 → 변경)를 먼저 검증할 수 있음.*
- **서버 액션에서 `user_id`를 서버가 채우고 `.eq("user_id", user.id)`로 이중 방어.** *이유: 서버 액션은 UI를 거치지 않고 POST로 직접 호출될 수 있어, RLS만 믿지 않고 애플리케이션 레벨에서도 소유권을 확인.*
- **통화 기준 결정: 원화 환산 단일 기준.** 지금은 통화별 매입금액 합계만 표시하고, 원화 환산 총액은 시세·환율 연동 시 추가. *이유: 환율 데이터가 아직 없어 환산값을 만들면 근거 없는 숫자가 됨.*
- **현금 자산군은 평단 입력을 비활성하고 1로 저장.** *이유: 현금에 평단 개념이 없으며, 강제 입력을 만들지 않는다는 원칙(귀찮게 하지 않는다)에 맞춤.*
- 에러 문구에 적색을 쓰지 않음. *이유: 이 제품에서 적색은 '수익'을 뜻하므로 의미 충돌.*

### 2026-08-24 — 1단계(뼈대) 구축

- **CLAUDE.md 생성** — 매매 실행·추천 문장·기사 전문 수집·크롤링·시크릿 노출 금지 등 하드룰과 디자인·문체 규칙을 한 파일에 고정. *이유: 세션이 바뀌어도 원칙이 흔들리지 않게 하기 위해.*
- **Next.js 16.3.2 (App Router) + Tailwind v4 + TypeScript 셋업**, 문서와 같은 루트에 배치. *이유: Vercel 루트 디렉터리 설정이 단순하고, 작업 중 기획 문서를 항상 옆에 두기 위해.*
- **Pretendard Variable self-host + `next/font/local`** (`src/app/fonts/`). *이유: 외부 CDN 의존 없이 오프라인 개발·CLS 제거. 전체 한글 글리프 포함이라 약 2MB.*
- **디자인 토큰을 `globals.css`의 Tailwind `@theme`에 정의** — bg/fg/muted/line + gain(적)/loss(청) 6색, 라운드 8px 이하, `tabular` 유틸(tabular-nums). 다크모드 대응 코드는 넣지 않음. *이유: DESIGN.md의 색·타이포 규칙을 코드 레벨에서 강제하고, 다크모드는 ROADMAP상 추후 과제이므로.*
- **Supabase Auth (Google OAuth) + `@supabase/ssr`** — 브라우저/서버 클라이언트, `/login`, `/auth/callback`, `/auth/signout`, 세션 갱신·미인증 리다이렉트용 `src/proxy.ts`. *이유: 처음부터 유저 분리 전제(2차 지인 공유)이고, Next.js 16에서 미들웨어 파일명이 proxy로 바뀌었기 때문.*
- **DB 스키마 `supabase/migrations/0001_init.sql`** — ARCHITECTURE §5의 공용 8개 + 유저별 6개 테이블, 전 테이블 RLS 활성화, 유저 테이블은 `auth.uid() = user_id` 4정책. 금액·수량 `numeric`, 시각 `timestamptz`, `briefings`에 일자 unique(멱등 재시도용). *이유: RLS를 나중에 붙이면 누락이 생기므로 스키마 생성과 동시에 적용.*
- **`profiles` 테이블 추가** (ARCHITECTURE의 `users(id, ...)` 구현). `auth.users`에 컬럼을 붙일 수 없어 1:1 테이블 + 가입 트리거로 대체. *이유: Supabase 표준 방식이며 스키마 설계 변경이 아님.*
- **Vercel 배포 완료** — https://study-phi-ten.vercel.app (GitHub: hyungyucreator/study, main 브랜치 자동 배포). 외부 점검 결과 `/` → 307 → `/login` 인증 가드 동작, Pretendard woff2가 `immutable` 캐시로 서빙됨. *이유: 1단계 완료 기준에 배포 파이프라인 동작 확인이 포함되므로.*
- **OAuth 콜백 에러 처리 수정** — 실패 시 오는 `error`/`error_description`을 그대로 로그인 화면에 표시하도록 변경(이전에는 전부 "인증 코드가 없다"로 뭉갬). *이유: 테스트 사용자 미등록(`access_denied`) 같은 실제 원인이 감춰져 디버깅이 불가능했음.*
- **Vercel 배포 준비** — `vercel.json`(framework nextjs, region icn1), `.env.example`, README 실행 절차. *이유: 서울 리전이 KIS·국내 데이터 소스와 가깝고 지연이 작음.*

### 2026-08-24
- 프로젝트 문서 세트 초안 작성 (PRODUCT / ARCHITECTURE / DESIGN / ROADMAP)
- 주요 설계 결정:
  - 매매 실행 기능 배제, 리밸런싱 주문서로 대체 (과잉거래 방지 + 법적 경계)
  - ETF 카탈로그 코어/틸트 2층 + 국면 태그
  - 브리핑 뉴스 필터 기준 = 직전 브리핑 생성 시각 이후 발행분
  - 국면 지표는 전용 화면 대신 브리핑 내 "오늘의 온도" 4지표로 시작
  - 퀴즈·커리큘럼·thesis 강제 입력 제외
- 브리핑을 2부 구성으로 확장: 1부 시장·경제(자산군 함의 구조) + 2부 오늘의 세계(정치·사회·국제·테크, 맥락·전망 구조) — 투자 전용이 아닌 종합 시사 도구로 목적 확장. 억지 투자 함의 연결 금지 원칙 추가
